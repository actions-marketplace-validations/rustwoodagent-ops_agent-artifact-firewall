name: New rule proposal
about: Suggest a new deterministic scanner rule
labels: [rules]
body:
  - type: input
    id: title
    attributes:
      label: Rule title
    validations:
      required: true
  - type: textarea
    id: risk
    attributes:
      label: Risk detected
    validations:
      required: true
