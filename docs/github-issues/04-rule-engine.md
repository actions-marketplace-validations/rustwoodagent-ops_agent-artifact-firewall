# Issue — Build initial deterministic rule engine

## Goal
Detect obvious prompt, secret, execution, MCP, and hook risks.

## Acceptance criteria
- CLI behavior is documented.
- Tests or fixtures cover the new behavior.
- README or docs are updated.

## Labels
`priority:p0`, `launch`
