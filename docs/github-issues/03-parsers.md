# Issue — Add parsers for Markdown, JSON, YAML, TOML, shell, and MCP config

## Goal
Turn discovered files into normalized objects that rules can evaluate.

## Acceptance criteria
- CLI behavior is documented.
- Tests or fixtures cover the new behavior.
- README or docs are updated.

## Labels
`priority:p0`, `launch`
