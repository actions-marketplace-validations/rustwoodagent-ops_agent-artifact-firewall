# Issue — Add reporters: terminal, JSON, SARIF, Markdown

## Goal
Make AGENT-ARTIFACT-FIREWALL useful locally and in GitHub code scanning.

## Acceptance criteria
- CLI behavior is documented.
- Tests or fixtures cover the new behavior.
- README or docs are updated.

## Labels
`priority:p0`, `launch`
